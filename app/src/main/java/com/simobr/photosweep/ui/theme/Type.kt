package com.simobr.photosweep.ui.theme

import androidx.compose.material3.Typography

/**
 * Material 3 defaults for now. Type is deliberately untouched until there are real screens
 * to size it against — overriding a scale before anything uses it just creates numbers
 * nobody has looked at.
 */
val PhotoSweepTypography = Typography()
