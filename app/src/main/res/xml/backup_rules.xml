<?xml version="1.0" encoding="utf-8"?>
<!-- Backup is disabled in the manifest (allowBackup="false"): everything Photo Sweep
     stores is a local cache of MediaStore state, and restoring a stale copy of it onto a
     different device would make the app reason about photos that are not there. -->
<full-backup-content />
