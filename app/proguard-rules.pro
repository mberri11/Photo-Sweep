# Photo Sweep has no reflection-based serialization and no backend models, so the default
# android-optimize rules plus the AAR-supplied rules (Room, Play Services Ads, UMP) suffice.
# Add rules here only when a real release build proves something is being stripped.
